services:
  api:
    build: ./backend
    ports:
      - "4000:4000"
    environment:
      - PORT=4000
    volumes:
      - ./backend/data:/app/data
  web:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - api
